{
    "name": "deploy",
    "dependencies": {},
    "engines": {
        "node": "^16"
    },
    "scripts": {
        "start": "node node_modules/@sap/hdi-deploy/deploy.js"
    },
    "cds": {
        "hana": {
            "deploy-format": "hdbtable"
        },
        "requires": {
            "db": {
                "kind": "hana"
            }
        }
    },
    "devDependencies": {
        "@sap/hdi-deploy": "^4.3.0"
    }
}
